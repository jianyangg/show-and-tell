{
  "name": "runner-frontend-workspace",
  "private": true,
  "workspaces": [
    "app"
  ],
  "scripts": {
    "dev": "yarn workspace runner-frontend dev",
    "build": "yarn workspace runner-frontend build",
    "preview": "yarn workspace runner-frontend preview",
    "lint": "yarn workspace runner-frontend lint"
  }
}
